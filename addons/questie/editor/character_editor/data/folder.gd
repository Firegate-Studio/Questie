tool
extends Resource

# the folder identifier
export(String) var id

# the name of the folder
export(String) var name

# the identifier of the parent folder of this one
export(String) var parent

# children indetifiers
export(Array) var children
